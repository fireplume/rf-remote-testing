Install the files in this folder on your target.

If you're manually starting the server on the target, you might have to export PYTHONPATH as in:

export PYTHONPATH=$PYTHONPATH:/opt/robot/my-project

so that it knows where to find the libraries.
